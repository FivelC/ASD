#include "monom.h"
#include <cctype>

void expression(std::string exem, unsigned int& x_degre, unsigned int& y_degre, unsigned int& z_degre, double& coef){
	Stack<char> split;

	
	/*for (int i = 0; i < exem.size() - 1; i++) {
		char tmp;
		int queue = 0; 
		if (isdigit(exem[i])) {
			tmp += exem[i];
		}
		else if (isalpha(exem[i])) { 
			if (queue == 0) {
				coef = tmp;
			}
		}
	}*/
}